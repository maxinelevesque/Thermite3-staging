# strat_probe fixture — binary_search.ensures.None
shape: forall_in
source: binary_search.ensures.None
origin: corpus
hit: true
--- production ---
forall i . 0 <= i < len(haystack) => idx(haystack, i) != needle
--- reference ---
forall w . (w < len(haystack) & 0 <= w) => idx(haystack, w) != needle
