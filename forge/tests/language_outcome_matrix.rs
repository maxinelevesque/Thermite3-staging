#[path = "../src/outcome_matrix.rs"]
mod outcome_matrix;

// The executable matrix tests live beside the closed classifier so later
// concrete stage adapters can reuse its vocabulary without duplicating it.
