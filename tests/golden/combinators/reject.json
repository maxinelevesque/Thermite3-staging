{
  "note": "Each program PARSES cleanly (thermite-syntax accepts the syntax — combinator calls are generic call exprs) but the thermite-spec validator must REJECT it with the named SpecError variant, because it steps outside the SpecTherm cage (§4.2). Hand-derived (R-CHAR-3). The `expected` variant names mirror .design/spec/spectherm-combinators.md REQ-4; the builder may rename variants but the REJECT outcome + the cause must hold.",
  "cases": [
    { "name": "unknown_combinator",   "expected": "UnknownCombinator", "program": "fn f(xs: &[u32]) -> u32 ! pure requires frobnicate(xs) ensures result == 0 { 0 }", "why": "frobnicate is not a registered combinator and not a declared spec fn — an arbitrary free-function call is forbidden in a contract (§4.2 locked cage)." },
    { "name": "wrong_arity",          "expected": "WrongArity",        "program": "fn f(xs: &[u32]) -> u32 ! pure requires sorted(xs, xs) ensures result == 0 { 0 }", "why": "sorted has arity 1; called with 2 args." },
    { "name": "wrong_arg_kind",       "expected": "WrongArgKind",      "program": "fn f(xs: &[u32]) -> u32 ! pure requires true ensures forall_in(xs, 5) { 0 }", "why": "forall_in's 2nd arg must be a predicate closure (Pred); a literal 5 is not a closure." },
    { "name": "forall_below_wrong_arity", "expected": "WrongArity",   "program": "fn f(xs: &[u32]) -> u32 ! pure requires true ensures forall_below(xs, |x| x < 5) { 0 }", "why": "forall_below has arity 3 (Slice, Index, Pred); called with 2 (missing the Index)." },
    { "name": "nested_combinator_in_closure", "expected": "NestedCombinator", "program": "fn f(xs: &[u32], ys: &[u32]) -> u32 ! pure requires true ensures forall_in(xs, |x| exists_in(ys, |y| y == x)) { 0 }", "why": "#40 flat-closure-fragment rule: a combinator's predicate-closure body may NOT contain another combinator call (anonymous nested quantifier). forall_in's closure here calls exists_in -> reject. Nested quantification must be a named spec fn." }
  ]
}
