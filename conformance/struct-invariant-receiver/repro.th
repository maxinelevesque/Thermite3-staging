struct State {
  panic_latched: bool,
  reschedule_pending: bool,
} keeps !panic_latched || !reschedule_pending

fn clear(s: State) -> State
  ! pure
  requires true
  ensures !result.reschedule_pending
{
  State {
    panic_latched: s.panic_latched,
    reschedule_pending: false,
  }
}
