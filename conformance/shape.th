enum Shape {
  Circle(u64),
  Rect { w: u64, h: u64 },
}

fn is_circle(s: Shape) -> bool
  ! pure
  requires true
  ensures result == (s is Circle)
{
  match s {
    Circle(r)     => true,
    Rect { w, h } => false,
  }
}
