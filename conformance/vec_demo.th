fn checked_get(v: Vec<u64>, i: usize) -> u64
  ! pure
  requires i < v.len()
  ensures result == v.get(i)
{
  v.get(i)
}

fn push_one(v: Vec<u64>, x: u64) -> Vec<u64>
  ! alloc
  requires v.len() < 1_000_000
  ensures result.len() == v.len() + 1
{
  let mut v2 = v;
  v2.push(x);
  v2
}
