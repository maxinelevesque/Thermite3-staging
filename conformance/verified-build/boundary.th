#[boundary("ext::foreign_identity")]
fn foreign_identity(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
;

fn boundary_root(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{
  foreign_identity(x)
}
