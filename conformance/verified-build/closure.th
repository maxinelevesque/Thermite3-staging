fn helper(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{
  x
}

fn closure_root(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{
  helper(x)
}

fn unrelated(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{
  x
}
