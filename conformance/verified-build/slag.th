#[slag(reason = "test crossing", owner = "thermite", review = "required")]
fn vendored(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{ x }

fn slag_root(x: u64) -> u64
  ! pure
  requires true
  ensures result == x
{ vendored(x) }
