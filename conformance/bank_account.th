struct Account {
  balance: u64,
} keeps balance <= 1_000_000

fn deposit(a: Account, amount: u64) -> Account
  ! pure
  requires a.balance + amount <= 1_000_000
  ensures result.balance == a.balance + amount
{
  Account { balance: a.balance + amount }
}
